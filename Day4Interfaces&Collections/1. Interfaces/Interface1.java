public interface Interface1 {
    // Interface: a contract that specifies what the class should do
    // Used to achieve abstration and multiple inheritance in java

    // All member of an interface are public, static and final

    int count = 5;
    void display1();
}
